import React from "react";
import LogbookBlock from "./blocks/LogbookBlock";

export default class LogbookOnly extends React.PureComponent<{}, {}> {

    render() {
        return <LogbookBlock />
    }

}